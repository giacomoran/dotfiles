#cloud-config

#:

users:
  - name: giacomoran
    groups: sudo
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    ssh_authorized_keys:
      # SSH public key for tiramisu
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJjL/ZlZCuKyJC345XIDkUo0/MDVvPB5McUXBjr57woa
ssh_pwauth: false

#:

runcmd:
  ##: Tailscale
  - ["sh", "-c", "curl -fsSL https://tailscale.com/install.sh | sh"]
  - [
      "sh",
      "-c",
      "echo 'net.ipv4.ip_forward = 1' | sudo tee -a /etc/sysctl.d/99-tailscale.conf && echo 'net.ipv6.conf.all.forwarding = 1' | sudo tee -a /etc/sysctl.d/99-tailscale.conf && sudo sysctl -p /etc/sysctl.d/99-tailscale.conf",
    ]
  # WARN: Replace YOUR_AUTH_KEY_HERE with your actual auth key
  # Generate one at https://login.tailscale.com/admin/settings/keys
  - ["tailscale", "up", "--auth-key=YOUR_AUTH_KEY_HERE"]
  - ["tailscale", "set", "--ssh"]

#:

final_message: |
  cloud-init completed!
  Version: $version
  Timestamp: $timestamp
  Uptime: $uptime

  Next steps:
    1. SSH into the host
    2. Enter the container: docker exec -it rocm /bin/bash
    3. Run: curl -fsSL https://raw.githubusercontent.com/giacomoran/dotfiles/remote/lerobot-amd/container-setup.sh | bash
